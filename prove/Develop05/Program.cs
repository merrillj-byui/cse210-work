using System;

class Program
{
    static void Main(string[] args)
    {
        // Instantiate the Goal Manager
        GoalManager goalManager = new GoalManager();
        
        // Start the Goal Manager running
        goalManager.Start();
    }
}